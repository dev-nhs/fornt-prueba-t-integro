import { PacienteAcompanante } from './paciente-acompanante';

describe('PacienteAcompanante', () => {
  it('should create an instance', () => {
    expect(new PacienteAcompanante()).toBeTruthy();
  });
});
