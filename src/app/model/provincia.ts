export class Provincia {
  public descripcion_provincia: string;
  public codigo_provincia: string;

  constructor(descripcion: string, codigo: string) {
    this.descripcion_provincia = descripcion;
    this.codigo_provincia = codigo;
  }
}
